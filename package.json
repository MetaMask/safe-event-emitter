{
  "name": "safe-event-emitter",
  "version": "1.0.1",
  "description": "A EventEmitter that isolates the emitter from errors in handlers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "events": "^3.0.0"
  }
}
